# TEMPLATE ONLY — this is NOT a working config and contains no secret.
# Get a real one: account.protonvpn.com -> Downloads -> WireGuard configuration
# (free server, name < 15 chars). Then fill in the values below and install it as
# /rw/config/wg0.conf in sys-split (chmod 600). NEVER commit the real file.
[Interface]
PrivateKey = REPLACE_WITH_YOUR_PRIVATE_KEY
Address = 10.2.0.2/32
# Do NOT add a DNS line — DNS goes through Tor.
Table = off

[Peer]
PublicKey = REPLACE_WITH_SERVER_PUBLIC_KEY
AllowedIPs = 0.0.0.0/0
Endpoint = REPLACE_SERVER_IP:51820
